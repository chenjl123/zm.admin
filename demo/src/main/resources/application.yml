server:
  port: 8003
spring:
  http:
    encoding:
      force: true
      charset: UTF-8
      enabled: true
  application:
    name: demo-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config